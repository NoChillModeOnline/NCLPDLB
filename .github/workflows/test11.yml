name: Test11
on:
  workflow_dispatch:
jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir -p data && echo "model" > data/model.zip
      - uses: actions/upload-artifact@v3
        with:
          name: my-artifact
          path: data/model.zip
